% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/02_inequality_indices.R
\name{calc_concentration_index}
\alias{calc_concentration_index}
\title{Calculate Concentration Index}
\usage{
calc_concentration_index(
  data,
  health_var,
  group_var,
  weight_var,
  rank_var = NULL,
  type = c("standard", "erreygers", "wagstaff")
)
}
\arguments{
\item{data}{A data frame with health and group columns.}

\item{health_var}{Name of the health variable column (character).}

\item{group_var}{Name of the socioeconomic group column (ordered integer,
1 = most deprived).}

\item{weight_var}{Name of the population share column (sums to 1).}

\item{rank_var}{Name of the socioeconomic rank variable (ridit scores,
0 = lowest, 1 = highest). If \code{NULL}, computed from \code{group_var}
and \code{weight_var} using ridit scoring.}

\item{type}{Concentration index variant: \code{"standard"} (Kakwani),
\code{"erreygers"} (bounded), or \code{"wagstaff"} (normalised).}
}
\value{
A named list with \code{ci} (concentration index), \code{se},
  and \code{type}.
}
\description{
Measures the degree to which a health variable is concentrated among
socioeconomically advantaged or disadvantaged groups. A negative value
indicates the health variable (e.g., illness) is concentrated among the
deprived; a positive value indicates concentration among the advantaged.
}
\examples{
df <- tibble::tibble(
  group      = 1:5,
  mean_hale  = c(60, 63, 66, 69, 72),
  pop_share  = rep(0.2, 5)
)
calc_concentration_index(df, "mean_hale", "group", "pop_share")
}
\references{
Erreygers G (2009). Correcting the Concentration Index. Journal of Health
Economics 28(2): 504-515. \doi{10.1016/j.jhealeco.2008.02.003}
}
