% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/02_inequality_indices.R
\name{calc_all_inequality_indices}
\alias{calc_all_inequality_indices}
\title{Calculate all inequality indices in one call}
\usage{
calc_all_inequality_indices(
  data,
  health_var,
  group_var,
  weight_var,
  epsilon_values = c(0.5, 1, 2)
)
}
\arguments{
\item{data}{A data frame with health and group columns.}

\item{health_var}{Name of the health variable column (character).}

\item{group_var}{Name of the socioeconomic group column (ordered integer,
1 = most deprived).}

\item{weight_var}{Name of the population share column (sums to 1).}

\item{epsilon_values}{Numeric vector of \eqn{\varepsilon} values for the
Atkinson index (default: \code{c(0.5, 1, 2)}).}
}
\value{
A tibble with columns \code{index}, \code{value}, and
  \code{description}.
}
\description{
Convenience wrapper that computes SII, RII, concentration index, Atkinson
index (for multiple \eqn{\varepsilon} values), and Gini coefficient and
returns them as a tidy data frame.
}
\examples{
df <- tibble::tibble(
  group      = 1:5,
  mean_hale  = c(60, 63, 66, 69, 72),
  pop_share  = rep(0.2, 5)
)
calc_all_inequality_indices(df, "mean_hale", "group", "pop_share")
}
